module SessionsHelper

  # # Logs in the given user.
  # def sign_in(user)
  #   session[:user_id] = user.id
  # end

  # Remembers a user in a persistent session.
  # def remember(user)
  #   user.remember
  #   cookies.permanent.signed[:user_id] = user.id
  #   cookies.permanent[:remember_key] = user.remember_key
  # end
end
